import { RouteNames } from "../../../stores/routeStore";
import "./projects.css";

export default function Projects() {
  return (
    <section id={`${RouteNames.projects}_id`} className="PROJECTS"></section>
  );
}
